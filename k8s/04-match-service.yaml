apiVersion: apps/v1
kind: Deployment
metadata:
  name: match-service
  namespace: game-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: match-service
  template:
    metadata:
      labels:
        app: match-service
    spec:
      containers:
        - name: match-service
          image: ghcr.io/YOUR_GITHUB_USERNAME/match-service:latest
          ports:
            - containerPort: 50052
          envFrom:
            - configMapRef:
                name: game-backend-config
            - secretRef:
                name: game-backend-secrets
          env:
            - name: GRPC_PORT
              value: "50052"
          readinessProbe:
            grpc:
              port: 50052
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            grpc:
              port: 50052
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: match-service
  namespace: game-backend
spec:
  selector:
    app: match-service
  ports:
    - port: 50052
      targetPort: 50052
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: match-service-hpa
  namespace: game-backend
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: match-service
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
